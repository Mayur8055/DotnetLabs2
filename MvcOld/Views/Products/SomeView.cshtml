@model MvcOld.Models.Product

@{
    ViewBag.Title = "SomeView";

}
<h2>SomeView</h2>
@{
    if (Model.ItemCode != 0)
    {
        Html.RenderPartial("Edit", Model);

    }
    else
    {
        Html.RenderPartial("AddProduct", Model);
    }
}

